html
    head
        title=title
    body
        h1=message
        form(class="form-signin" method="POST" action="../login")
            //-add id
            label( for="inputID" class="sr-only") Verify your ID:
            input(type="text" name="id" id="inputID" class="form-control" placeholder="ID" required autofocus)

            //-Get report button
            - var buttonClass=['btn', 'btn-lg', 'btn-primary', 'btn-block'];
            button(class=buttonClass type="submit") Get report

            - var pClasses=['mt-5', 'mb-3', 'text-muted'];